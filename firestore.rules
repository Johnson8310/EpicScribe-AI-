rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to all documents.
    // This is suitable for an app without user-specific data protection.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
